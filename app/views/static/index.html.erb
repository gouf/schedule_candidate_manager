<h1>Static#index</h1>
<p>Find me in app/views/static/index.html.erb</p>

<%= link_to 'Login with Google', auth_at_provider_path(provider: :google) %>
<p id="notice"><%= notice %></p>
<p id="alert"><%= alert %></p>

<%= current_user.present? ? image_tag(current_user.profile_image_url) : nil %>
